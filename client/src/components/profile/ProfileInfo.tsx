import React, { FC } from 'react';
import Header from '../Header';

const ProfileInfo: FC = () => {
    return (
        <>
        <Header />
        ProfileInfo
        </>
    );
}

export default ProfileInfo;